[ req ]
default_bits        = 4096
prompt              = no
default_md          = sha256
distinguished_name  = dn
req_extensions      = req_ext

[ dn ]
CN = etcd

[ req_ext ]
subjectAltName = @alt_names

[ alt_names ]
DNS.1 = control-plane
DNS.2 = localhost
IP.1  = 10.20.0.10
IP.2  = 127.0.0.1
